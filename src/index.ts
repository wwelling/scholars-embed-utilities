import { Page, WindowDimensions } from './model';

import {
    formalize,
    pagination,
    compileTemplate,
    getTemplateFunction,
    getParsedTemplateFunction,
    initializeTemplateHelpers
} from './utilities';

export {
    Page,
    WindowDimensions,
    formalize,
    pagination,
    compileTemplate,
    getTemplateFunction,
    getParsedTemplateFunction,
    initializeTemplateHelpers
};
